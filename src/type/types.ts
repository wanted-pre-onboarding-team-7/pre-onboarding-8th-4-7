export interface FullComment {
  id: number;
  profile_url: string;
  auther: string;
  content: string;
  createdAt: string;
}
export interface CommentExceptId {
  id: number;
  profile_url: string;
  auther: string;
  content: string;
  createdAt: string;
}
